# Web Scraping Configuration

# Protocol to use (http or https)
PROTOCOL=https

# Base domain to crawl
DOMAIN_BASE=localhost

# Start domain (defaults to DOMAIN_BASE if not set)
DOMAIN_START=localhost

# Start path for crawling
START_PATH=/

# Port number
PORT=8080

# Output directory for saved files
BASE_OUTPUT_DIR=./site_archive

# Override existing files (true or false)
OVERRIDES=false

# Interval between requests in milliseconds
INTERVAL_BETWEEN_REQUESTS=5000

# Interval factor for request delays
INTERVAL_FACTOR=0.5

# Request timeout in milliseconds
TIMEOUT=60000

# Proxy server (if needed)
PROXY=http://hefesto:55455
